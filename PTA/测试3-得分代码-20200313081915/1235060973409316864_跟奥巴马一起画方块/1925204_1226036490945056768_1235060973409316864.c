#include<stdio.h>
int main()
{
 int a,b,i,n;
  char c;
   scanf("%d ",&a);
    c=getchar();
     if(a%2==0)b=a/2;
      else b=(a+1)/2;
       for(i=1;i<=b;i++)
        {
          for(n=1;n<=a;n++){
             putchar(c);}
               printf("\n");
                }
                 return 0;
                 }