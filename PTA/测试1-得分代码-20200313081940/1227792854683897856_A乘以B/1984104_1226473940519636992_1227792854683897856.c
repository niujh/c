#include<stdio.h>
void main()
{
    int a,b;
    while(1)
    {
    scanf("%d %d",&a,&b);
    if(a>= 100 || b>= 100 || a<= -100 || b<= -100)
    {
        break;
    }
    else
    {
        break;
    }
    }
    printf("%d\n",a*b) ;
}