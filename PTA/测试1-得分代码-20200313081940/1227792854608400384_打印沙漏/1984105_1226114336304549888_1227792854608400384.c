#include<stdio.h>
#include<math.h>
int main()
{
    int a;
    char b;
    scanf("%d %c",&a,&b);
    if(a>1000)return 0;
    if(a<1)return 0;
    int i=1;
    int c=1;
    int d=1;
    if(a>3)
        {
            printf("%c\n%d",b,a-1)；
        }
    else
        {
            while(a>c)
        }
}