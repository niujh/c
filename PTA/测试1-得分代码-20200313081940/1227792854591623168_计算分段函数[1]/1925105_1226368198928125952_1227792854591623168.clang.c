#include <stdio.h>
int main()
 {  
      float x,y;
           if x=0;
                    y=0;
                         else  y=1/x;
                                 printf("%.1f%.1f",f(x)=y);  
                                      return 0; }